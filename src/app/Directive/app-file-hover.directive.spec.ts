import { AppFileHoverDirective } from "./app-file-hover.directive";

describe('AppFileHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFileHoverDirective();
    expect(directive).toBeTruthy();
  });
});
